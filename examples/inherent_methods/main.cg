// Inherent methods + method call lowering (originally v0.0.9).

mod util;

use util::Counter;

fn main() -> i32 {
    let mut c: Counter = Counter { n: 0 };
    c.inc(5);
    c.inc(37);
    c.get()
}
