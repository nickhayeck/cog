// Basic LLVM codegen smoke test (originally v0.0.9).

fn add(a: i32, b: i32) -> i32 {
    a + b
}

fn main() -> i32 {
    let x: i32 = add(40, 2);
    if x == 42 {
        x
    } else {
        0
    }
}
