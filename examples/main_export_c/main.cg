// Demonstrates explicit C ABI entry point:
// `fn[export(C)] main(argc: i32, argv: const* const* u8) -> i32`.

fn[export(C)] main(argc: i32, argv: const* const* u8) -> i32 {
    argv;
    argc;
    0
}

