pub enum Mode {
    Off,
    On(i32),
}

pub trait Sink {
    fn write(self: mut* Self, b: u8) -> ();
}

pub struct CounterSink {
    pub n: i32,
}

impl Sink for CounterSink {
    fn write(self: mut* Self, b: u8) -> () {
        self.n = self.n + (b as i32);
        ()
    }
}

